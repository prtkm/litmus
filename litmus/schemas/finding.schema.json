{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://litmus.dev/schemas/finding.schema.json",
  "title": "LITMUS Finding",
  "description": "A verifier's verdict on one claim (DESIGN §6.3, §14). A FAIL MUST ship a recompute_script + expected_output (DESIGN §3.2).",
  "type": "object",
  "required": ["verifier_id", "status", "trust_tier", "verifier_kind"],
  "additionalProperties": false,
  "properties": {
    "verifier_id": { "type": "string" },
    "claim_id": { "type": ["string", "null"] },
    "status": { "type": "string", "enum": ["pass", "fail", "inconclusive", "error"] },
    "trust_tier": {
      "type": "string",
      "enum": ["deterministic_confirmed", "calibrated_synthesized", "advisory_assisted", "routed_to_human"]
    },
    "verifier_kind": { "type": "string", "enum": ["prebuilt", "templated", "synthesized", "assisted"] },
    "severity": { "type": ["string", "null"], "enum": ["A", "B", "C", null] },
    "message": { "type": "string" },
    "discrepancy": { "type": ["string", "null"] },
    "reported": {},
    "computed": {},
    "evidence": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "quote": { "type": ["string", "null"] },
        "location": { "type": "object" },
        "recompute_script": { "type": ["string", "null"] },
        "expected_output": { "type": ["string", "null"] },
        "script_dependencies": { "type": "array", "items": { "type": "string" } }
      }
    },
    "details": { "type": "object" }
  },
  "allOf": [
    {
      "if": { "properties": { "status": { "const": "fail" } } },
      "then": {
        "properties": {
          "severity": { "type": "string", "enum": ["A", "B", "C"] },
          "evidence": {
            "required": ["recompute_script", "expected_output"],
            "properties": {
              "recompute_script": { "type": "string", "minLength": 1 },
              "expected_output": { "type": "string" }
            }
          }
        },
        "required": ["severity", "evidence"]
      }
    }
  ]
}
