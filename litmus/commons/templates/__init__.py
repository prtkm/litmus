"""Scaffold templates for the contribution SDK (DESIGN §9).

The ``.py`` files here are not modules to import — they are source templates with
``__VERIFIER_*__`` placeholders that :func:`litmus.commons.sdk.scaffold_verifier` substitutes.
This package marker just lets the SDK locate them via ``importlib.resources``.
"""
